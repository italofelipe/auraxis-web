/**
 * Tools composable.
 *
 * Provides Vue Query hooks for fetching the tools catalog with feature flag support
 * and mock data for development/testing when enabled.
 */
export { useToolsCatalogQuery } from "./useTools";
export type { ToolsCatalog } from "~/types/contracts";
