/**
 * Feature access composable.
 *
 * Provides reactive access state for checking whether the authenticated user
 * has access to a given feature based on entitlements.
 */
export { useFeatureAccess } from "./useFeatureAccess";
export type { FeatureAccessResult } from "./useFeatureAccess";
